export default class {

    constructor(PLAYER_TYPE) {
        this.totalTokenCount = 7;
        this.unusedTokenCount = 7;
        this.PLAYER_TYPE = PLAYER_TYPE;
        this.Name;
    }

    toString(){
        return this.Name;
    }
}